<ContentControl x:Class="GiViewer.App.Controls.Editor"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:local="clr-namespace:GiViewer.App.Controls"
                xmlns:app="clr-namespace:GiViewer.App"
                xmlns:vm="clr-namespace:GiViewer.App.ViewModels"
                mc:Ignorable="d" 
                d:DataContext="{d:DesignInstance Type=vm:EditorViewModel}"
                d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="12, 0, 12, 12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="180"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition MinWidth="180"/>
        </Grid.ColumnDefinitions>

        <!-- 功能区 -->
        <Border Grid.Row="0" Grid.Column="0"
                Grid.ColumnSpan="3"
                Margin="0, 0, 0, 12"
                CornerRadius="4" Height="60"
                BorderThickness="1, 1, 1, 2"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
            
            
        </Border>

        <!-- 节点视图 -->
        <TreeView Name="View" Grid.Row="1" Grid.Column="0">
            <TreeView.Style>
                <Style TargetType="{x:Type TreeView}" BasedOn="{StaticResource {x:Type TreeView}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsEmpty, Mode=OneWay}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=IsEmpty, Mode=OneWay}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TreeView.Style>
        </TreeView>
        
        <TextBlock Name="EmptyTip"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Grid.Row="1" Grid.Column="0"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsEmpty, Mode=OneWay}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=IsEmpty, Mode=OneWay}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- 分割线 -->
        <GridSplitter Grid.Row="1" Grid.Column="1"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"/>

        <!-- 右侧面板 -->
        <Border Grid.Row="1" Grid.Column="2"
                CornerRadius="4"
                BorderThickness="1, 1, 1, 2"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6*"/>
                </Grid.RowDefinitions>

                <Border Grid.RowSpan="2"
                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"/>

                <TextBlock Grid.Row="0"
                           Name="PropertyTitle"
                           Margin="12,8,0,0"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            </Grid>
        </Border>
    </Grid>
</ContentControl>
